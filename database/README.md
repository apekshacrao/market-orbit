# Database Schema & Migrations

PostgreSQL database schemas, migrations, and seed scripts for AI Marketing Performance Analyzer.

## Structure

- `migrations/`: Sequential SQL migration files
- `seed/`: Sample campaign CSV data and seed SQL scripts
- `schema.sql`: Complete consolidated schema
