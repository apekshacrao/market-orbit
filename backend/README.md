# Backend - AI Marketing Performance Analyzer

FastAPI backend providing REST APIs for authentication, dataset upload, analysis dispatch, and result retrieval.

## Setup & Run

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```
